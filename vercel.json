{
  "framework": "nextjs",
  "installCommand": "npm ci --no-audit --fund=false",
  "buildCommand": "NEXT_TELEMETRY_DISABLED=1 CI=1 npm run build",
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1",
    "CI": "1"
  },
  "ignoreCommand": "bash scripts/vercel_ignore_build.sh"
}
