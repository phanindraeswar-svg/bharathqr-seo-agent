# Cannibalization Audit — 2026-06-09

## Verdict
PASS — Duplicate keyword overlaps are either absent or explicitly managed by `data/cannibalization_resolution.json`.

## Unmanaged Exact Keyword Conflicts
- None.

## Managed Keyword Overlaps
- `brochure qr code` is intentionally split across support pages: ['/materials/brochures', '/use-cases/share-brochure']
- `contact qr code` is intentionally split across support pages: ['/materials/business-cards', '/tools/vcard-qr-generator', '/use-cases/share-contact-details']
- `event qr code` is intentionally split across support pages: ['/tools/event-qr-generator', '/use-cases/manage-events']
- `event registration qr` is intentionally split across support pages: ['/tools/event-qr-generator', '/use-cases/manage-events']
- `google review qr code` is intentionally split across support pages: ['/templates/google-review-poster', '/tools/google-review-qr-generator', '/use-cases/collect-reviews']
- `hotel menu qr` is intentionally split across support pages: ['/materials/menus', '/tools/menu-qr-generator']
- `menu qr table tent` is intentionally split across support pages: ['/materials/table-tents', '/templates/restaurant-table-tent']
- `qr code on brochure` is intentionally split across support pages: ['/materials/brochures', '/templates/brochure-qr-template']
- `restaurant menu qr` is intentionally split across support pages: ['/materials/menus', '/templates/restaurant-qr-template']
- `restaurant table qr code` is intentionally split across support pages: ['/materials/table-tents', '/templates/restaurant-table-tent']
- `upi qr standee` is intentionally split across support pages: ['/materials/shop-counter', '/templates/upi-qr-standee']
- `wedding qr code` is intentionally split across support pages: ['/tools/event-qr-generator', '/use-cases/manage-events']

## Keyword Ownership Registry Conflicts
- None.
