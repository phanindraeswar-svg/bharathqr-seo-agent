import Head from 'next/head';
import Link from 'next/link';
import industryClusters from '../../data/industry_clusters.json';
import toolClusters from '../../data/tool_clusters.json';

export async function getStaticPaths(){ return { paths: Object.values(industryClusters).map(i=>({params:{slug:i.slug}})), fallback:false }; }export async function getStaticProps({params}){ const item=Object.values(industryClusters).find(i=>i.slug===params.slug); if(!item) return {notFound:true}; return {props:{item}, revalidate:86400}; }
export default function SolutionPage({item}){ const url=`https://bharathqr.com/solutions/${item.slug}`; const desc=`${item.primary_keyword} for Indian businesses. Use BharathQR tools to solve ${item.pain_points.slice(0,2).join(', ')}.`; const tools=(item.tools||[]).map(k=>toolClusters[k]).filter(Boolean); const schema={'@context':'https://schema.org','@type':'WebPage',name:item.title,description:desc,url}; return <><Head><title>{item.title} — BharathQR</title><meta name="description" content={desc.slice(0,155)}/><link rel="canonical" href={url}/><meta property="og:title" content={`${item.title} — BharathQR`}/><meta property="og:description" content={desc.slice(0,155)}/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/></Head><main style={{maxWidth:1000,margin:'0 auto',padding:'1.5rem 1rem'}}><section style={{background:'linear-gradient(135deg,#ECFDF5,#FFFFFF)',border:'1px solid #D1FAE5',borderRadius:24,padding:'clamp(1.25rem,5vw,2.5rem)',textAlign:'center'}}><p style={{color:'#1D9E75',fontWeight:800,margin:0}}>Business QR Solution</p><h1 style={{fontSize:'clamp(2rem,7vw,3.2rem)',margin:'0.5rem 0'}}>{item.title}</h1><p style={{color:'#4B5563',fontSize:18,lineHeight:1.6}}>Use QR codes to {item.benefits.slice(0,3).join(', ')} for {item.industries.join(', ')}.</p></section><section style={{marginTop:28,display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(250px,1fr))',gap:16}}><div style={{border:'1px solid #E5E7EB',borderRadius:18,padding:20}}><h2>Pain points solved</h2><ul>{item.pain_points.map(p=><li key={p}>{p}</li>)}</ul></div><div style={{border:'1px solid #E5E7EB',borderRadius:18,padding:20}}><h2>Business benefits</h2><ul>{item.benefits.map(b=><li key={b}>{b}</li>)}</ul></div></section><section style={{marginTop:28}}><h2>Recommended BharathQR tools</h2><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(220px,1fr))',gap:14}}>{tools.map(t=><Link key={t.slug} href={t.tool_url} style={{border:'1px solid #D1FAE5',borderRadius:16,padding:18,textDecoration:'none',color:'#111827',background:'#F0FDF4'}}><strong style={{color:'#065F46'}}>{t.title}</strong><p style={{color:'#4B5563'}}>{t.primary_keyword} for {item.industries[0]} use cases.</p><span style={{color:'#1D9E75',fontWeight:800}}>Open tool →</span></Link>)}</div></section><section style={{marginTop:28,lineHeight:1.7}}><h2>Where to place QR codes</h2><p>Print QR codes on billing counters, table tents, menus, brochures, posters, packaging, reception desks and WhatsApp profiles. Test every QR code before printing.</p><h2>FAQ</h2><h3>Which QR code is best for {item.industries[0]}?</h3><p>Start with the tool that solves your biggest pain point: UPI QR for payments, Google Review QR for reviews, WhatsApp QR for enquiries, and WiFi QR for guests.</p><h3>Is BharathQR free?</h3><p>Yes. BharathQR tools are free, mobile-first and do not require login.</p></section></main></>; }
